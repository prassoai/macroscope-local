---
name: macroscope-autoloop
description: Run the local review-fix-verify loop until the branch is clean.
---

Use the native `skill` tool with `name: "autoloop"` to load the installed Macroscope autoloop workflow, then follow it exactly. Resolve the skill through the host registry, not relative to the repository being reviewed.
